{
  "name": "mern-vote-tamil-nadu",
  "version": "1.0.0",
  "description": "Tamil Nadu Voting Application with MERN Stack",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd server && yarn start\" \"cd client && yarn start\"",
    "server": "cd server && yarn start",
    "client": "cd client && yarn start",
    "install-all": "yarn install && cd client && yarn install && cd ../server && yarn install"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "private": true
} 